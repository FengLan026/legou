const state = {
  addressId: ''
}
const mutations = {
  setAddressId(state, addressId) {
    state.addressId = addressId
  }
}
const actions = {
  
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
